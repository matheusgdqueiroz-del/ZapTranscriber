{
  "name": "zap-transcriber",
  "version": "1.0.2",
  "private": true,
  "description": "Extensão Chrome para transcrever mensagens de voz do WhatsApp Web.",
  "scripts": {
    "build": "node scripts/build-extension.mjs",
    "test": "node --test tests/*.test.js",
    "validate": "node scripts/validate-extension.mjs && node --check src/background/service-worker.js && node --check src/offscreen/inference.js && node --check src/page/bridge.js && node --check content/content.js && node --check popup/popup.js && node --check lib/audio-utils.js",
    "check": "npm run build && npm run test && npm run validate"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@huggingface/transformers": "3.8.1",
    "@wppconnect/wa-js": "4.6.0",
    "esbuild": "0.28.2"
  }
}
